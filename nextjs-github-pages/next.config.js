// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  output: 'export',
  basePath: '/Test-Next-Deploy-Pages',
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
